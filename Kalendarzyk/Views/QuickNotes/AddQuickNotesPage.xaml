<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Kalendarzyk.Views.QuickNotes.AddQuickNotesPage"
             Title="AddQuickNotesPage">
    <VerticalStackLayout>

        <!-- Entry for new type name -->
        <Entry             Margin="10"
                           Placeholder="...QUICK NOTE TITILE..." 
                           Text="{Binding QuickNoteTitle, Mode=TwoWay}" 
                           FontAttributes="Bold" FontSize="Body"
                           HorizontalTextAlignment="Center" 
                           BackgroundColor="{DynamicResource MainTextboxesBackgroundColor}" 
                           TextColor="{DynamicResource MainTextColor}" 
                           VerticalOptions="Center" 
                           HorizontalOptions="Center"  />
        <Entry             Margin="10"
                           Placeholder="...QUICK NOTE DESCRIPTION..." 
                           Text="{Binding QuickNoteTitle, Mode=TwoWay}" 
                           FontAttributes="Bold" FontSize="Body"
                           HorizontalTextAlignment="Center" 
                           BackgroundColor="{DynamicResource MainTextboxesBackgroundColor}" 
                           TextColor="{DynamicResource MainTextColor}" 
                           VerticalOptions="Center" 
                           HorizontalOptions="Center" MinimumWidthRequest="200" />
        <FlexLayout>
            <Button Text="COLOR"/>
            <Button Text="TYPE"/>

        </FlexLayout>
        <!--xxxxxxxxxxxxxxx-->


        <Button Style="{DynamicResource DangerButtonStyle}" WidthRequest="300" Text="DELETE QUICK NOTE  " IsVisible="{Binding IsEdit}" Command="{Binding DeleteAsyncSelectedMainEventTypeCommand}"/>
        <Button Margin="5" Text="{Binding SubmitMainTypeButtonText}" Command="{Binding SubmitAsyncMainTypeCommand}" HorizontalOptions="Center"/>

    </VerticalStackLayout>
</ContentPage>